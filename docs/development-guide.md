# Development Guide

## Prerequisites
- Node.js (version per Nuxt 4 requirements)
- Package manager: pnpm

## Setup
- Install dependencies: `pnpm install`

## Run
- Dev server: `pnpm dev` (http://localhost:3000)
- Build: `pnpm build`
- Preview prod build: `pnpm preview`

## Project Structure (observed, quick scan)
- `app/app.vue` — Nuxt root view (currently NuxtWelcome)
- No `server/`, `api/`, `components/`, `pages/` yet in this scan

## Deployment
- No deployment configuration detected yet. Follow Nuxt deployment guide when target platform is chosen.

## Testing
- No tests or test setup detected in quick scan.

## Notes
- Update this guide as soon as you add pages/components, API/server, or deployment scripts.
